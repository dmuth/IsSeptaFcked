
div#main

	h1.title #{title}

	//-
	//- Regional Rail Status
	//-
	div.status
		span Regional Rail is 
	div.status
		span(class = "#{rr_status_class}") #{rr_status}

	ul#trains
		if (rr_late)
			each train in rr_late
				li #{train}

	if (rr_message)
		div#message #{rr_message}
	p
	div#status-time.
		Train data last updated: #{rr_status_time}

	br
	br
	br

	//-
	//- Bus status
	//-
	div.status
		span Busses are
	div.status
		span(class = "#{bus_status_class}") #{bus_status}

	ul#busses
		if (bus_suspended)
			each bus in bus_suspended
				li #{bus}

	if (bus_message)
		div#message #{bus_message}
	p
	div#status-time.
		Bus data last updated: #{bus_status_time}


//-
//- Social widgets.
//-
include lib/facebook-sdk
include lib/google-plus-sdk
div.upper-left
	include lib/social-widgets

include lib/footer

//-
//- Our GitHub ribbon in the upper right.
//-
a(href="https://github.com/dmuth/IsSeptaFcked") <img style="position: absolute; top: 0; right: 0; border: 0;" src="https://s3.amazonaws.com/github/ribbons/forkme_right_red_aa0000.png" alt="Fork me on GitHub"></a>


