
body

	h1 #{title}

	div#status
		span Regional Rail is 
		span(class = "#{status_class}") #{status}

	ul#trains
		if (late)
			each train in late
				li #{train}

	if (message)
		div#message #{message}


